<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.project.parth.closestone.DislikedPlaces">

    <TextView
        android:id="@+id/disliked_title"
        android:text="@string/disliked_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30dp"
        android:layout_centerHorizontal="true"/>
    <TextView
        android:layout_marginTop="20dp"
        android:id="@+id/disliked_message"
        android:layout_below="@+id/disliked_title"
        android:text="@string/disliked_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"/>

    <ListView
        android:layout_marginTop="20dp"
        android:id="@id/android:list"
        android:layout_below="@+id/disliked_message"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:gravity="center"/>

    <ImageView
        android:layout_marginTop="20dp"
        android:layout_below="@id/android:list"
        android:id="@+id/add_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/plus_icon"
        android:layout_centerHorizontal="true"/>
    <EditText
        android:id="@+id/new_item_dislike"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/add_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <Button
        android:layout_marginTop="10dp"
        android:id="@+id/add_dislike"
        android:layout_below="@+id/new_item_dislike"
        android:text="Add New"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
</ScrollView>